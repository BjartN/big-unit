Unit testing framework for JavaScript.

The testing framework is NOT coupled to the presentation layer (TestGui.js).

The current GUI runs in Windows Script Host (WSH)

The project is in an early stage, and might be there forever.

